// UDD - Expresión Digital II
// s03 recap con todas las diferentes funciones


//Conocimientos hasta ahora:
//Funciones:
size(width, height);
//comentarios
point(x, y);
line(x1, y1, x2, y2);

// rect
rect(x, y, width, height);
rectMode(CORNER); CORNERS, CENTER,  RADIUS

// ellipse
ellipse(x, y, width, height);
ellipseMode(CENTER); RADIUS, CORNER, CORNERS

// relleno
noFill();
fill(rgb); //FFFFFF ? 
fill(rgb, alpha); //FFFFFF ?
fill(gray);
fill(gray, alpha);
fill(v1, v2, v3);
fill(v1, v2, v3, alpha);

// color stroke
noStroke();
stroke(rgb); //FFFFFF ?
stroke(rgb, alpha); //FFFFFF ?
stroke(gray);
stroke(gray, alpha);
stroke(v1, v2, v3);
stroke(v1, v2, v3, alpha):

// tipo de color
colorMode(RGB); HSB
colorMode(mode, max);

// tipo de stroke
strokeWeight(weight); 
strokeJoin(MITER); ROUND, BEVEL
strokeCap(ROUND); SQUARE, PROJECT


